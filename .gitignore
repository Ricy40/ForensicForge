# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/

# Virtual environments
.venv/
venv/
env/

# Test / cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Generated RAG vector store (rebuilt from knowledge/ on demand)
.chroma/

# Per-run `provision` output (Ansible role + Vagrantfile), one dir per run-id
generated/

# Packer build artifacts (built base box, not committed)
packer/builds/
packer_cache/
*.box

# Local Packer CLI binary download (not a project dependency to commit)
packer_*_windows_*/

# Molecule's ephemeral per-scenario state (inventory, instance config)
.molecule/

# Env / secrets
.env
.env.*

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db
